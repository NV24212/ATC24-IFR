# Environment variables for your application
# For local development, you can copy this file to .env.local and fill in the values.
# DO NOT commit .env.local to version control.

# Supabase Configuration
# The SUPABASE_URL and SUPABASE_ANON_KEY are public and can be stored here.
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# The SUPABASE_SERVICE_ROLE key is a secret and should NOT be stored in this file.
# It should be set as a secret or environment variable in your hosting provider's dashboard.
# Example: SUPABASE_SERVICE_ROLE=your_secret_service_role_key

# Discord OAuth Configuration
DISCORD_CLIENT_ID=your_discord_client_id
DISCORD_CLIENT_SECRET=your_discord_client_secret

# The DISCORD_REDIRECT_URI is optional and will be auto-detected in most environments.
# Only set this if the automatic detection is failing.
# DISCORD_REDIRECT_URI=http://localhost:3000/auth/discord/callback

# Session Secret (optional, a default will be used if not set)
SESSION_SECRET=a_strong_random_secret_string_for_sessions
